export class RoomManager {
    /**
     * RoomManager Responsibilities:
     * - ensuring spawn/extensions have energy
     * - spawning more creeps if needed
     * - repairing strucutures
     * - filling towers
     * - upgrading controller
     * - filling storage
     * - mining sources
     * - harvesting resources
     */

    private room_needs(room: Room) {

    }
}
